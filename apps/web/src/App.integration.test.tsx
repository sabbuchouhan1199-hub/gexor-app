import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { afterEach, expect, it, vi } from "vitest";
import { App } from "./App";
const json=(body:unknown,status=200)=>new Response(JSON.stringify(body),{status,headers:{"Content-Type":"application/json"}});
afterEach(()=>{localStorage.clear();vi.unstubAllGlobals()});
it("uses canonical authenticated conversation and execution routes",async()=>{const current={user:{userId:"u",email:"u@example.com",displayName:"User",status:"active",createdAt:"x",updatedAt:"x"},session:{sessionId:"s",userId:"u",status:"active",createdAt:"x",expiresAt:"y",lastSeenAt:"x"},workspace:{workspaceId:"workspace_1",ownerUserId:"u",name:"My Workspace",status:"active",createdAt:"x",updatedAt:"x"},membership:{membershipId:"m",workspaceId:"workspace_1",userId:"u",role:"owner",status:"active",createdAt:"x",updatedAt:"x"}};localStorage.setItem("gexor.session","token");const fetchMock=vi.fn().mockResolvedValueOnce(json(current)).mockResolvedValueOnce(json({conversations:[]})).mockResolvedValueOnce(json({conversationId:"conversation_1",workspaceId:"workspace_1",title:"Hello",status:"active",version:1,createdAt:"x",updatedAt:"x"},201)).mockResolvedValueOnce(json({messageId:"message_1",executionId:"execution_1",state:"accepted",requestId:"r",createdAt:"x",links:{execution:"/api/v1/executions/execution_1"}},202)).mockResolvedValueOnce(json({executionId:"execution_1",messageId:"message_1",conversationId:"conversation_1",requestId:"r",workspaceId:"workspace_1",requestedBy:"u",state:"completed",createdAt:"x",updatedAt:"y",response:{text:"Canonical reply"},links:{self:"/api/v1/executions/execution_1"}})).mockResolvedValueOnce(json({messages:[{messageId:"message_1",conversationId:"conversation_1",role:"user",text:"Hello",state:"complete",createdAt:"x"},{messageId:"message_1:assistant",conversationId:"conversation_1",role:"assistant",text:"Canonical reply",state:"complete",createdAt:"y"}]})).mockResolvedValue(json({conversations:[]}));vi.stubGlobal("fetch",fetchMock);const user=userEvent.setup();render(<App/>);const box=await screen.findByPlaceholderText("Message Gexor…");await user.type(box,"Hello");await user.keyboard("{Enter}");expect(await screen.findByText("Canonical reply")).toBeInTheDocument();expect(fetchMock.mock.calls.some(c=>String(c[0]).includes("/api/v1/conversations/conversation_1/messages"))).toBe(true);expect(fetchMock.mock.calls.some(c=>String(c[0]).includes("/api/v1/executions/execution_1"))).toBe(true)});
